% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_series}
\alias{export_series}
\title{Export NASCAR Series Data}
\usage{
export_series(series = c("cup", "nxs", "truck"), path, format = NULL, ...)
}
\arguments{
\item{series}{Character. The series to export. One of \code{"cup"},
\code{"nxs"}, or \code{"truck"}.}

\item{path}{Character. Path to the output file (including extension).}

\item{format}{Character. Output format: \code{"csv"}, \code{"parquet"}, or \code{NULL}.
If \code{NULL} (default), the format is guessed from the file extension of \code{path}.}

\item{...}{Additional arguments passed to \code{\link[utils:write.table]{utils::write.csv()}} (for CSV) or
\code{\link[arrow:write_parquet]{arrow::write_parquet()}} (for Parquet).}
}
\value{
Invisibly returns the exported data.
}
\description{
Downloads or loads the specified NASCAR series data and writes it
to a file in either CSV or Parquet format.
}
\examples{
\dontrun{
# Export Cup Series data to CSV
export_series("cup", "cup_data.csv")

# Export Truck Series data to Parquet
export_series("truck", "truck_data.parquet")
}

}
